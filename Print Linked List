### Aim: To implement a function that prints all elements of a given linked list in a single line, separated by spaces.

### Objective:
    -To understand the basic structure of a linked list.
    -To traverse a linked list efficiently.
    -To display the elements of a linked list in a structured format.

### Code:
class Solution {
public:
    void printList(Node *head) {
        Node* temp = head;
        while (temp) {
            cout << temp->data << " ";
            temp = temp->next;
        }
        cout << endl;
    }
};
